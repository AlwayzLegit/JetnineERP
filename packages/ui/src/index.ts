// Shared React components live here. shadcn/ui primitives are added per-app
// (their convention is to copy components into the consuming app), but
// composite/branded components shared across the super-admin, business, and POS
// surfaces belong in this package.

export {};
